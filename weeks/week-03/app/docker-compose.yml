version: '3.8'

services:
  comments-service:
    build: .
    container_name: comments-svc-s01
    ports:
      - "8298:8298"
    environment:
      - SERVICE_NAME=comments-svc-s01
      - SERVICE_PORT=8298
    restart: unless-stopped